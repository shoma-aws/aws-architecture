# 概要
CloudWatch AlarmをトリガーとしてLambdaを自動実行し、CloudWatch Logs・CloudWatch Metrics・CloudTrailから取得した情報を統合してAmazon Bedrock（Claude）へ送信することで、インフラの状態や障害の原因をAIが自動分析するシステムを構築しました。<br>
ログ・メトリクス・操作履歴を横断的に分析し、障害発生時の初動対応を支援することを目的としています。

# 作成理由
業務の中で、障害が発生した際にはまずログやメトリクスを確認し原因調査を行いますが、<br>
- 調査する範囲が担当者によって異なる
- 情報のまとめ方が人によってばらつく<br>
といった課題があり、原因特定のスピードや精度に差が出ると感じました。<br>
また、アプリケーションチームが利用しているサーバーで障害が発生した場合、
インフラチームによる調査を待つ必要があり、　
- 調査開始までに時間がかかる可能性がある
- 初動対応が遅れるリスクがある<br>
という運用上の課題もあると考えました。<br>
これらの背景から、ログ・メトリクス・操作履歴を統合し、AIによって原因分析を自動化することで、<br>
「調査の属人化解消」と「初動対応の迅速化」を目的として本システムを構築しました。

# 使用サービス
- Amazon EC2
- Amazon CloudWatch Logs
- Amazon CloudWatch Metrics
- Amazon CloudWatch Alarm
- AWS CloudTrail
- AWS Lambda
- Amazon Bedrock（Claude）

# 構成図
![](AI監視システム構成図.drawio.png)

# 処理フロー

1. EC2でアプリケーションが動作<br>
2. CloudWatch Logsにログが蓄積される<br>
3. CloudWatch MetricsでCPUなどのメトリクスを取得<br>
4. CloudWatch Alarmが閾値超過を検知<br>
5. Lambdaが自動起動<br>
6. Lambdaが以下の情報を取得<br>
   - ログ（CloudWatch Logs）
   - CPU使用率（CloudWatch Metrics）
   - 操作履歴（CloudTrail）<br>
7. Lambda内でデータを整形<br>
8. AIに渡すため自然言語へ変換<br>
9. Bedrock（Claude）で原因分析<br>
10. 分析結果をCloudWatch Logsに出力

# スクリーンショット
## 監視対象となるEC2インスタンスを作成しました。
AIによる監視・分析の対象となるWebサーバーを構築するため、Amazon EC2インスタンスを作成しました。
本システムでは、このEC2で生成されるログやCPU使用率などのメトリクスをCloudWatchへ収集し、AI分析に利用します。
![](./images/01.EC2.png)

## EC2にSSH接続をしました。
EC2インスタンスへSSH接続を行い、WebサーバーやCloudWatch Agentの構築・設定を実施するための作業環境を準備しました。
![](./images/02.SSH.png)

## nginxをインストールし、監視対象となるWebサーバーを構築しました。
Webサーバーとしてnginxをインストールしました。生成されるアクセスログ・エラーログをCloudWatch Logsへ送信し、AI分析の入力データとして利用します。
![](./images/03.nginx.png)

## EC2上にnginxを構築し、Webサーバーが正常に動作していることをブラウザから確認しました。
ブラウザからEC2上のnginxへアクセスし、Webサーバーが正常に稼働していることを確認しました。この正常な状態を基準として、後続の異常検知テストを実施します。
![](./images/04.nginx-2.png)

## CloudWatch Agentをインストールし、EC2のログをCloudWatch Logsへ送信する準備を行いました。
CloudWatch Agentをインストールし、EC2で生成されるログやメトリクスをAWSへ送信するための準備を行いました。
![](./images/05.cloudwatch-agent-install.png)

## EC2へCloudWatch Agentを導入し、CloudWatch LogsおよびCloudWatch Metricsの収集をしました。
CloudWatch Agentを設定し、nginxのログとEC2のCPU使用率などのメトリクスをCloudWatchへ収集できるよう構成しました。
![](./images/06.cloudwatch-agent-2.png)

## CloudWatch LogsへEC2のnginxアクセスログ・エラーログを送信し、ログ収集基盤を構築しました。
CloudWatch Logsへnginxのアクセスログ・エラーログを送信し、サーバー上のログをAWS上で一元管理できるログ収集基盤を構築しました。
![](./images/07.logGroup.png)

## EC2上で稼働するnginxのアクセスログをCloudWatch Logsへ送信し、AWS上で一元管理できるようにしました。
CloudWatch Logs上でアクセスログが正常に収集されていることを確認しました。Lambdaはこのログを取得し、AI分析に利用します。
![](./images/08.logGroup-2.png)

## CloudWatch Alarmから起動されるLambda関数で、CloudWatch Logs・CloudWatch Metrics・CloudTrailを取得し、Bedrockへ分析を依頼します。
CloudWatch Alarmをトリガーとして起動するLambda関数を作成しました。CloudWatch Logs・CloudWatch Metrics・CloudTrailの情報を収集・統合し、Amazon Bedrockへ分析を依頼します。
![](./images/10.Lambda.png)

## Lmabdaに許可ポリシーをアタッチしました。
LambdaがCloudWatch Logs・CloudWatch Metrics・CloudTrail・Amazon Bedrockへアクセスできるよう、必要なIAMポリシーをアタッチしました。
![](./images/11.Lambda-policy.png)

## LambdaがCloudWatch Logs・CloudWatch Metrics・CloudTrailの情報を取得し、Amazon Bedrock（Claude Sonnet 4.5）へ送信しました。AIが収集したデータを分析し、システムの状態・想定される原因・推奨対応を日本語で自動生成していることを確認しました。
LambdaがCloudWatch Logs・CloudWatch Metrics・CloudTrailから収集した情報をAmazon Bedrock（Claude Sonnet 4.5）へ送信し、AIによる障害分析を実行しました。システムの状態・想定される原因・推奨対応が日本語で自動生成されることを確認しました。
![](./images/12.Lambda-test.png)
![](./images/13.Lambda-test2.png)
![](./images/14.Lambda-test3.png)

## EC2へCPU負荷をかけ、CloudWatch Alarmが高負荷を正常に検知することを確認しました。
EC2へCPU負荷を発生させ、CPU使用率が70%を超えた際にCloudWatch Alarmが正常にALARM状態へ遷移することを確認しました。本システムでは、このアラームをトリガーとしてLambda関数を自動実行し、AIによる障害分析を開始します。
![](./images/15.CPU-huka.png)
![](./images/16.aleam.png)

## CPU高負荷を検知し、AIによる自動分析が実行されることを確認しました。
EC2へCPU負荷を与え、CloudWatch AlarmがCPU使用率の閾値超過を検知したことを契機に、Lambda関数が自動実行されました。<br>
LambdaはCloudWatch Metrics・CloudWatch Logs・CloudTrailから情報を取得してAmazon Bedrock（Claude Sonnet 4.5）へ送信し、CPU使用率が44%から78%へ急上昇していること、アプリケーションの応答速度低下やサービス影響の可能性があることを日本語で自動分析しました。
![](./images/17.ai-log1.png)
![](./images/18.ai-log2.png)
![](./images/19.ai-log3.png)
